# Activate test profile by default for all tests
spring.profiles.active=test

# Flyway — H2-compatible migrations for tests without explicit profile
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration/h2
