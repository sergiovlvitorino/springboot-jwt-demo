# Test application properties — overrides main application.properties for all tests
# Ensures Flyway uses H2-compatible migrations in @DataJpaTest and @SpringBootTest contexts

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration/h2
